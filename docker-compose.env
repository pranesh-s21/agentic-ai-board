# Used only by Docker Compose (not the Node app).
# Keeps Compose from parsing $... in NORTH_SESSION_COOKIE from the main .env.
POSTGRES_USER=board
POSTGRES_PASSWORD=board
POSTGRES_DB=board_governance
